# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: EUPL-1.2 OR GPL-3.0-or-later
# GitHub Release Creator
# 
# Purpose: Creates GitHub releases with  security features including
# artifact signing, SBOM generation, and checksum calculation using GitHub CLI.
#
# How it works:
# 1. Downloads all artifacts from the workflow run
# 2. Generates checksums (SHA-256) for all artifacts
# 3. Creates SBOM in multiple formats (CycloneDX, SPDX) if enabled
# 4. Signs artifacts with GPG if configured
# 5. Creates GitHub release with changelog using gh CLI
# 6. Uploads all artifacts, checksums, signatures, and SBOMs to release
#
# Security controls:
# - GPG signing of all release artifacts
# - Checksum generation for integrity verification
# - SBOM for supply chain transparency
# - Support for attestation attachment
#
# Required Secrets:
#   RELEASE_GPG_PRIVATE_KEY: For GPG import/signing in release creation
#   RELEASE_GPG_PASSPHRASE: Passphrase for the imported release GPG key
#   RELEASE_TOKEN: For release creation and asset upload
#
# Required Permissions:
#   contents: write      # Create releases + upload assets
#
# Generated Files:
#   - checksums.sha256: SHA-256 checksums for all artifacts
#   - *.sig: GPG signatures for each artifact
#   - sbom.json: CycloneDX/SPDX Software Bill of Materials
name: Create GitHub Release
on:
  workflow_call: # yamllint disable-line rule:truthy
    inputs:
      release-name:
        description: "Release title shown in GitHub Releases"
        required: false
        default: ""
        type: string
      checkout-ref:
        description: "Git ref to checkout (branch or tag)"
        required: false
        default: "main"
        type: string
      attach-artifacts:
        description: "Glob patterns for artifacts to attach (newline or comma separated)"
        required: false
        type: string
      sboms:
        description: "SBOMs to generate and attach: all | none | comma-list of build,analyzed-artifact,analyzed-container (default: all)"
        required: false
        default: "all"
        type: string
      sign-artifacts:
        description: "GPG sign all artifacts"
        required: false
        default: true
        type: boolean
      draft:
        description: "Create as draft release"
        required: false
        default: false
        type: boolean
      config-plan-json:
        description: "Typed config-plan-json emitted by config parse-artifacts. SBOM generation uses it to preserve per-artifact working directories."
        required: true
        type: string
      artifact-transfer-plan-json:
        description: "Typed artifact transfer plan emitted by plan release."
        required: true
        type: string
      changelog-config:
        description: "Path to changelog configuration file"
        required: false
        default: ".github-templates/templates/gitcliff/default.toml"
        type: string
      make-latest:
        description: "Mark as latest release (true, false, legacy)"
        required: false
        default: "true"
        type: string
      egress-policy:
        description: "Egress policy for the Harden Runner step. Default `audit` (visibility-only). Promote to `block` for strict posture — pair with `egress-allowed-endpoints` covering at minimum: api.github.com:443, objects.githubusercontent.com:443, *.actions.githubusercontent.com:443, github.com:443, codeload.github.com:443. Add fulcio.sigstore.dev:443 + rekor.sigstore.dev:443 + tuf-repo-cdn.sigstore.dev:443 + token.actions.githubusercontent.com:443 when sign.method=sigstore. Add your KMS provider (e.g. *.amazonaws.com, *.googleapis.com, *.vault.azure.net) when sign.method=kms."
        required: false
        default: "audit"
        type: string
      egress-allowed-endpoints:
        description: "Multi-line space/newline-separated list of `host:port` endpoints permitted under `egress-policy: block`. Ignored when policy is `audit`. See the egress-policy description for the recommended baseline."
        required: false
        default: ""
        type: string
      runtime-image:
        description: "reusable-ci runtime image (full — needs syft for SBOM generation). Optional override; default uses the matching v3.0.0 runtime image."
        required: false
        default: "ghcr.io/diggsweden/reusable-ci-runtime:v3.0.0"
        type: string
    # All `required: false` — `secrets: inherit` callers unchanged.
    secrets:
      RELEASE_GPG_PRIVATE_KEY:
        description: "Armored OpenPGP private key (used when sign.method=gpg in artifacts.yml)"
        required: false
      RELEASE_GPG_PASSPHRASE:
        description: "Passphrase for RELEASE_GPG_PRIVATE_KEY"
        required: false
      RELEASE_TOKEN:
        description: "Token with contents:write permission for creating the GitHub Release"
        required: false
permissions:
  contents: read # Best Security practice. Jobs only get read as base, and then permissions are added as needed
jobs:
  generate-changelog:
    name: Generate Release Changelog
    permissions:
      contents: read
    uses: ./.github/workflows/generate-changelog.yml
    with:
      branch: ${{ inputs['checkout-ref'] }}
      runtime-image: ${{ inputs['runtime-image'] }}
      git-cliff-args: "--latest"
      output-file: "ReleasenotesTmp"
      upload-as-artifact: true
      artifact-name: "release-changelog"
      changelog-config: ${{ inputs['changelog-config'] }}
  create-release:
    name: Create GitHub Release
    needs: [generate-changelog]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    container:
      image: ${{ inputs['runtime-image'] }}
    permissions:
      actions: read
      contents: write
      packages: read
      id-token: write
    steps:
      - name: Harden GitHub runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: ${{ inputs['egress-policy'] }}
          allowed-endpoints: ${{ inputs['egress-allowed-endpoints'] }}
      # Defense-in-depth: refuse PR-context triggers. RELEASE_TOKEN +
      # RELEASE_GPG_PRIVATE_KEY land in env on this job and must
      # never run with PR-HEAD code. reusable-ci is pre-baked.
      - name: Refuse PR-context trigger (publish/release secrets in scope)
        run: reusable-ci validate event-context
      - name: Checkout repository with moved tag
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CHECKOUT_REF: ${{ inputs['checkout-ref'] }}
          CHECKOUT_FETCH_ALL_REFS: "true" # full history + all tags
        run: reusable-ci platform checkout
      - name: Import GPG key
        # GPG agent setup is only needed when artifact signing uses the
        # GPG backend. Sigstore (keyless OIDC) and KMS (cosign + external
        # provider) don't read $GPG_PRIVATE_KEY at all — running this
        # step with no secret would fail loud, and running it with the
        # secret would needlessly decrypt a key the flow doesn't use.
        # The plan precomputes sign.imports_gpg_key so this gate carries the
        # intent ("import the key") rather than hard-coding the method name.
        if: ${{ inputs['sign-artifacts'] && fromJSON(inputs['config-plan-json']).sign.imports_gpg_key }}
        id: import-gpg
        env:
          GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          GIT_USER_SIGNINGKEY: "true"
        run: reusable-ci release gpg import
      - name: Download Release Changelog
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          ARTIFACT_NAME: release-changelog
          ARTIFACT_DIR: .
        run: reusable-ci artifact download
      - name: Download planned release artifacts
        env:
          ARTIFACT_TRANSFER_PLAN_JSON: ${{ inputs['artifact-transfer-plan-json'] }}
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: reusable-ci release download-artifacts
      - name: Compute effective attach-artifacts (auto-include extracted binaries)
        # If extract.binary produced any files under ./release-artifacts/binaries/,
        # append a glob so they get checksummed, signed, and attached to the
        # GitHub Release alongside the user-specified globs. Without this,
        # callers who declare extract.binary in artifacts.yml would see binaries
        # only as run-page CI artefacts (90-day retention) but not on the
        # permanent Release page — silent footgun. The append is non-destructive
        # to the user's own attach-artifacts input.
        id: attach-effective
        env:
          USER_ATTACH: ${{ inputs['attach-artifacts'] }}
        run: reusable-ci release attachments plan
      - name: Resolve release metadata
        id: release-metadata
        env:
          VERSION: ${{ inputs['release-name'] }}
          REPOSITORY: ${{ github.repository }}
        run: reusable-ci release resolve metadata
      - name: Generate CISA-layer SBOMs
        if: &if_has_sboms ${{ inputs.sboms != 'none' }}
        env:
          CONFIG_PLAN_JSON: ${{ inputs['config-plan-json'] }}
          VERSION: ${{ steps.release-metadata.outputs.version-no-v }}
          SBOMS: ${{ inputs.sboms }}
        run: reusable-ci sbom assemble
      - name: Assemble release files
        env:
          CONFIG_PLAN_JSON: ${{ inputs['config-plan-json'] }}
          ARTIFACT_TRANSFER_PLAN_JSON: ${{ inputs['artifact-transfer-plan-json'] }}
          ATTACH_ARTIFACTS: ${{ steps.attach-effective.outputs.attach-artifacts }}
          PROJECT_NAME: ${{ steps.release-metadata.outputs.project-name }}
          VERSION: ${{ steps.release-metadata.outputs.version }}
          RELEASE_ASSEMBLY: .reusable-ci/release-assembly.json
        run: reusable-ci release assemble
      - name: Create SBOM ZIP
        if: *if_has_sboms
        env:
          PROJECT_NAME: ${{ steps.release-metadata.outputs.project-name }}
          VERSION: ${{ steps.release-metadata.outputs.version }}
          RELEASE_ASSEMBLY: .reusable-ci/release-assembly.json
        run: reusable-ci release sbom-zip
      - name: Generate checksums
        env:
          RELEASE_ASSEMBLY: .reusable-ci/release-assembly.json
        run: reusable-ci release checksums
      - name: Sign artifacts
        if: ${{ inputs['sign-artifacts'] }}
        # Backend selected by sign.method in artifacts.yml:
        #   gpg      → reads $GPG_PRIVATE_KEY / $GPG_PASSPHRASE (in-process openpgp signer)
        #   sigstore → cosign + GHA OIDC (no secret; needs id-token: write — granted above)
        #   kms      → cosign + $SIGN_KEY URI (KMS auth handled by cosign + KMS provider)
        # Secrets scoped to this step so they don't leak to later steps.
        env:
          SIGN_METHOD: ${{ fromJSON(inputs['config-plan-json']).sign.method }}
          SIGN_KEY: ${{ fromJSON(inputs['config-plan-json']).sign.key }}
          SIGN_OIDC_ISSUER: ${{ fromJSON(inputs['config-plan-json']).sign.oidc_issuer }}
          GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
          GPG_KEY_ID: ${{ steps.import-gpg.outputs.keyid }}
          RELEASE_ASSEMBLY: .reusable-ci/release-assembly.json
        run: reusable-ci release sign
      - name: Prepare release notes
        env:
          SOURCE_FILE: ReleasenotesTmp
          TARGET_FILE: release-notes.md
          RELEASE_VERSION: ${{ inputs['release-name'] }}
          RELEASE_COMMIT: ${{ github.sha }}
        run: reusable-ci release notes
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
          TAG_NAME: ${{ inputs['release-name'] }}
          RELEASE_NAME: ${{ inputs['release-name'] }}
          REPOSITORY: ${{ github.repository }}
          DRAFT: ${{ inputs.draft }}
          MAKE_LATEST: ${{ inputs['make-latest'] }}
          RELEASE_ASSEMBLY: .reusable-ci/release-assembly.json
          RELEASE_NOTES_FILE: release-notes.md
        run: reusable-ci release create
      - name: Upload release artifacts summary
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-artifacts-${{ github.run_id }}
          path: |
            .reusable-ci/release-assembly.json
            release-files/
            ReleasenotesTmp
            release-notes.md
          retention-days: "30"
          if-no-files-found: ignore
      - name: Cleanup GPG key
        if: always()
        env:
          GPG_FINGERPRINT: ${{ steps.import-gpg.outputs.fingerprint }}
        run: reusable-ci release gpg cleanup
